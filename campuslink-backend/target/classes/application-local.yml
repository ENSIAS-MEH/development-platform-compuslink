# ──────────────────────────────────────────────────────────────────
# Local overrides — this file is in .gitignore, never committed.
# Spring Boot automatically loads this when you run with:
#   -Dspring.profiles.active=local
# or set SPRING_PROFILES_ACTIVE=local in your IDE.
# ──────────────────────────────────────────────────────────────────

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/campuslink_db
    username: campuslink         # ← change to your local DB user
    password: campuslink         # ← change to your local DB password

# Uncomment when auth is implemented:
# app:
#   jwt:
#     secret: your-very-long-random-secret-minimum-256-bits
#     expiration-ms: 86400000

server.port=8083
spring.application.name=colocation-service
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/compuslink_colocation}
spring.datasource.username=${DB_USERNAME:mourad}
spring.datasource.password=${DB_PASSWORD:}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false
eureka.client.service-url.defaultZone=${EUREKA_URI:http://localhost:8761/eureka/}
jwt.secret=${JWT_SECRET:your256bitsecretheyforjwtsigningpurposesexactly}
spring.servlet.multipart.max-file-size=10MB
app.upload.dir=${UPLOAD_DIR:uploads}
app.base-url=${BASE_URL:http://localhost:8080}

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: colocation-uploads-pvc
  namespace: compuslink
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: colocation-service
  namespace: compuslink
spec:
  replicas: 1
  selector:
    matchLabels:
      app: colocation-service
  template:
    metadata:
      labels:
        app: colocation-service
    spec:
      containers:
        - name: colocation-service
          image: compuslink/colocation-service:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8083
          env:
            - name: JAVA_TOOL_OPTIONS
              value: "-XX:MaxRAMPercentage=60.0 -XX:+UseSerialGC -XX:MaxMetaspaceSize=128m -Xss512k"
            - name: DB_URL
              value: "jdbc:postgresql://postgres:5432/compuslink_colocation"
            - name: DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: compuslink-secrets
                  key: db-username
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: compuslink-secrets
                  key: db-password
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: compuslink-secrets
                  key: jwt-secret
            - name: EUREKA_URI
              valueFrom:
                configMapKeyRef:
                  name: compuslink-config
                  key: EUREKA_URI
            - name: UPLOAD_DIR
              valueFrom:
                configMapKeyRef:
                  name: compuslink-config
                  key: UPLOAD_DIR
            - name: BASE_URL
              valueFrom:
                configMapKeyRef:
                  name: compuslink-config
                  key: BASE_URL
          volumeMounts:
            - name: coloc-uploads-volume
              mountPath: /app/uploads
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
          readinessProbe:
            tcpSocket:
              port: 8083
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 12
          livenessProbe:
            tcpSocket:
              port: 8083
            initialDelaySeconds: 120
            periodSeconds: 20
            failureThreshold: 3
      volumes:
        - name: coloc-uploads-volume
          persistentVolumeClaim:
            claimName: colocation-uploads-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: colocation-service
  namespace: compuslink
spec:
  ports:
    - port: 8083
      targetPort: 8083
  selector:
    app: colocation-service

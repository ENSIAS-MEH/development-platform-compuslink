apiVersion: apps/v1
kind: Deployment
metadata:
  name: messaging-service
  namespace: compuslink
spec:
  replicas: 1
  selector:
    matchLabels:
      app: messaging-service
  template:
    metadata:
      labels:
        app: messaging-service
    spec:
      containers:
        - name: messaging-service
          image: compuslink/messaging-service:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8086
          env:
            - name: DB_URL
              value: "jdbc:postgresql://postgres:5432/compuslink_messaging"
            - name: DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: compuslink-secrets
                  key: db-username
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: compuslink-secrets
                  key: db-password
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: compuslink-secrets
                  key: jwt-secret
            - name: EUREKA_URI
              valueFrom:
                configMapKeyRef:
                  name: compuslink-config
                  key: EUREKA_URI
---
apiVersion: v1
kind: Service
metadata:
  name: messaging-service
  namespace: compuslink
spec:
  ports:
    - port: 8086
      targetPort: 8086
  selector:
    app: messaging-service